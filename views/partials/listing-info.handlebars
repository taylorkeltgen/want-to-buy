<div class="card text-center" style="width: 20 rem;">
  <div class="meta">
    <img class="icons" width="150px" height="150px" src="{{category.image_id}}" alt="{{category.label}}">
  </div>
  <div class="card-body">
    <h5 class="card-title"><a href="/listings/{{id}}">{{item}}</a></h5>
    <p class="card-text">{{listing.description}}</p>
  </div>
</div>